# Development & Deployment (Quick Scan)

Prerequisites:
- Node >=14 (package.json engines)

Install:
- `npm install`

Build:
- `npm run build` (tsc && chmod +x dist/index.js)

Run:
- `npm run start` (ts-node index.ts)
- `npm run dev` (ts-node index.ts)
- `npm run help` (ts-node index.ts --help)

Tests:
- (none defined in package.json scripts)

Environment:
- .env, .env.example (API keys and config)

Deployment/CI:
- None detected (no Dockerfiles or CI pipelines in quick scan)

Contribution:
- No CONTRIBUTING.md found in quick scan
