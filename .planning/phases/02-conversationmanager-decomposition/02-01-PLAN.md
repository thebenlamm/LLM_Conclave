---
phase: 02-conversationmanager-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/orchestration/__tests__/ConversationManager.judge.integration.test.ts
  - src/core/ConversationHistory.ts
  - src/core/__tests__/ConversationHistory.test.ts
  - src/core/ConversationManager.ts
autonomous: true
requirements: [TEST-01, CONV-01]
must_haves:
  truths:
    - "Judge evaluation integration tests exist and pass, covering context compression scenario"
    - "ConversationHistory class owns all history manipulation: groupHistoryByRound, compressHistory, formatEntryAsMessage, prepareMessagesForAgent, prepareMessagesWithBudget, prepareMessagesWithRoundCompression"
    - "ConversationManager no longer contains those history methods directly — it delegates to ConversationHistory"
    - "All 938+ existing tests still pass after extraction"
  artifacts:
    - path: "src/orchestration/__tests__/ConversationManager.judge.integration.test.ts"
      provides: "Integration tests for judge evaluation with context compression"
      min_lines: 100
    - path: "src/core/ConversationHistory.ts"
      provides: "ConversationHistory class with all history manipulation methods"
      exports: ["ConversationHistory"]
      min_lines: 200
    - path: "src/core/__tests__/ConversationHistory.test.ts"
      provides: "Unit tests for ConversationHistory"
      min_lines: 80
  key_links:
    - from: "src/core/ConversationManager.ts"
      to: "src/core/ConversationHistory.ts"
      via: "this.history = new ConversationHistory(...)"
      pattern: "new ConversationHistory"
    - from: "src/core/ConversationHistory.ts"
      to: "src/utils/ContextOptimizer.ts"
      via: "import for compression"
      pattern: "import.*ContextOptimizer"
---

<objective>
Establish the safety-net tests for judge evaluation (TEST-01), then extract the ConversationHistory class (CONV-01) as the first decomposition step.

Purpose: The judge evaluation path with context compression is the riskiest part of the CONV-03 extraction. Tests must exist before we touch it. ConversationHistory is the foundational extraction — both AgentTurnExecutor and JudgeEvaluator will use it.

Output: Judge integration tests, ConversationHistory class, unit tests, and a thinner ConversationManager.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-conversationmanager-decomposition/02-CONTEXT.md

@src/core/ConversationManager.ts
@src/types/index.ts
@src/utils/ContextOptimizer.ts
@src/utils/TokenCounter.ts
@src/orchestration/__tests__/ConversationManager.integration.test.ts

<interfaces>
<!-- Key types the executor needs -->

From src/types/index.ts:
```typescript
export interface DiscussionHistoryEntry {
  role: 'user' | 'assistant';
  content: string;
  speaker: string;
  model?: string;
  error?: true;
  errorDetails?: string;
  compressed?: true;
  positionSummary?: string;
}

export interface Config {
  // ... config fields used by ConversationManager
}
```

From src/utils/ContextOptimizer.ts:
```typescript
export class ContextOptimizer {
  static compressRoundMessages(messages: ...): ...
  // Used by ConversationManager.compressHistory() and prepareMessagesWithRoundCompression()
}
```

Key ConversationManager history methods to extract (lines 1021-1304):
- `groupHistoryByRound()` (line 1037) — Groups entries by round using judge guidance/compressed delimiters
- `compressHistory()` (line 1069) — Compresses middle rounds via ContextOptimizer when over token threshold
- `formatEntryAsMessage(entry, compress)` (line 1136) — Formats a DiscussionHistoryEntry as {role, content}
- `prepareMessagesForAgent()` (line 1149) — Builds message array for agent calls
- `prepareMessagesWithRoundCompression()` (line 1192) — Progressive round compression variant
- `prepareMessagesWithBudget(agentName)` (line 1241) — Budget-aware message preparation
- `getHistoryTokenThreshold()` (line 1021) — Returns token threshold based on config

State accessed by these methods:
- `this.conversationHistory` (DiscussionHistoryEntry[])
- `this.config` (Config — for agents, context optimization settings)
- `this.currentRound` (number)
- `this.messageCache` (any[]) + `this.lastCacheUpdateIndex` (number)
- `this.agentSubstitutions` (Map)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add judge evaluation integration tests with context compression</name>
  <files>src/orchestration/__tests__/ConversationManager.judge.integration.test.ts</files>
  <read_first>
    - src/orchestration/__tests__/ConversationManager.integration.test.ts (existing test patterns and mock setup — reuse `createSetup` helper pattern)
    - src/core/ConversationManager.ts (lines 1638-2050 for judgeEvaluate, conductFinalVote, buildCaseFile, prepareJudgeContext, bestEffortJudgeResult)
    - src/core/ConversationManager.ts (lines 1069-1135 for compressHistory)
    - src/types/index.ts (DiscussionHistoryEntry type at line 195)
  </read_first>
  <action>
Create `src/orchestration/__tests__/ConversationManager.judge.integration.test.ts` with these test scenarios:

1. **Judge evaluation with compressed history** — Set up a conversation with 4+ rounds of agent responses (enough to trigger compression at default threshold), then call `judgeEvaluate`. Verify:
   - Judge receives the full discussion text (not truncated)
   - Judge response includes `shouldContinue` and `synthesis` fields
   - History compression happened (check that some entries have `compressed: true`)

2. **Judge shallow-agreement detection** — Set up history where agents use phrases like "I agree", "I concur", "well said" (at least 2 matches). Call `judgeEvaluate`. Verify the judge prompt includes the shallow agreement warning.

3. **Judge quoting detection** — Set up history where current-round agent responses contain direct quotes (">") from other agents. Call `judgeEvaluate`. Verify the judge prompt includes the quoting warning.

4. **bestEffortJudgeResult fallback** — Set up a scenario where the judge provider throws an error. Verify `startConversation` still returns a result with `consensus` field (from bestEffortJudgeResult fallback).

5. **conductFinalVote with compressed history** — Set up max_rounds=2 with 2 rounds of agent responses plus compression, then let `startConversation` complete. Verify the final vote result contains `winner` and `reasoning`.

Mock setup pattern: Follow the existing `createSetup` helper in `ConversationManager.integration.test.ts`. Use `jest.mock` for ProviderFactory, EventBus, SpeakerSelector. Create mock agent and judge providers with sequenced responses via `mockResolvedValueOnce`.

For compression-triggering scenarios, create agent responses that are long enough (500+ characters each) and set up enough rounds (4+) with `min_rounds: 1, max_rounds: 4` so the token threshold is reached. The default threshold is 100000 tokens but can be lowered by mocking `TokenCounter.countTokens` to return inflated counts.

The test file must use the same mock patterns as the existing integration test: `jest.mock('../../providers/ProviderFactory')`, `jest.mock('../../core/EventBus')`, suppress console output.
  </action>
  <verify>
    <automated>cd /Users/benlamm/Workspace/llm_conclave && npx jest --testPathPattern="ConversationManager.judge.integration" --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `src/orchestration/__tests__/ConversationManager.judge.integration.test.ts` exists
    - File contains at least 5 `it(` test cases
    - File contains `describe('Judge evaluation with context compression'` or similar top-level describe
    - All tests pass (exit code 0)
    - File contains `jest.mock('../../providers/ProviderFactory')`
    - File contains `compressed` assertion (testing compression happened)
  </acceptance_criteria>
  <done>Integration tests for judge evaluation with context compression all pass. Safety net established for CONV-03 extraction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract ConversationHistory class from ConversationManager</name>
  <files>src/core/ConversationHistory.ts, src/core/__tests__/ConversationHistory.test.ts, src/core/ConversationManager.ts</files>
  <read_first>
    - src/core/ConversationManager.ts (full file — you are extracting from it)
    - src/types/index.ts (DiscussionHistoryEntry at line 195, Config at line 180)
    - src/utils/ContextOptimizer.ts (used by compressHistory and prepareMessagesWithRoundCompression)
    - src/utils/TokenCounter.ts (used by getHistoryTokenThreshold)
    - src/orchestration/__tests__/ConversationManager.integration.test.ts (must still pass)
    - src/orchestration/__tests__/ConversationManager.judge.integration.test.ts (must still pass — created in Task 1)
  </read_first>
  <behavior>
    - Test: `groupHistoryByRound` with no entries returns empty array
    - Test: `groupHistoryByRound` with judge guidance entry creates round boundary
    - Test: `groupHistoryByRound` with compressed entry creates round boundary
    - Test: `formatEntryAsMessage` with compress=false returns full content
    - Test: `formatEntryAsMessage` with compress=true and positionSummary returns summary
    - Test: `prepareMessagesForAgent` returns array of {role, content} messages
    - Test: `getHistoryTokenThreshold` returns default 100000 when no config override
  </behavior>
  <action>
**Create `src/core/ConversationHistory.ts`:**

```typescript
export default class ConversationHistory {
  constructor(
    private entries: DiscussionHistoryEntry[],
    private config: Config,
    private getCurrentRound: () => number,
    private getAgentSubstitutions: () => Map<string, { original: string; fallback: string; reason: string }>
  ) {}
```

The class takes ownership of:
- `entries` array reference (same array as ConversationManager.conversationHistory — shared by reference, not copied)
- Config reference for accessing agents, context optimization settings
- `getCurrentRound` callback to get the current round number (owned by ConversationManager)
- `getAgentSubstitutions` callback to get substitution map (owned by ConversationManager)

Move these methods from ConversationManager into ConversationHistory (preserving exact logic):
1. `groupHistoryByRound()` (lines 1037-1061)
2. `getHistoryTokenThreshold()` (lines 1021-1035) — reference `this.config` for threshold
3. `compressHistory()` (lines 1069-1135) — uses ContextOptimizer, TokenCounter, getHistoryTokenThreshold, groupHistoryByRound
4. `formatEntryAsMessage(entry, compress)` (lines 1136-1148) — private helper
5. `prepareMessagesForAgent()` (lines 1149-1191) — builds message array for agent calls
6. `prepareMessagesWithRoundCompression()` (lines 1192-1240) — progressive round compression
7. `prepareMessagesWithBudget(agentName)` (lines 1241-1304) — budget-aware variant

Also move the caching state:
- `messageCache: any[]`
- `lastCacheUpdateIndex: number`

Use `export default class ConversationHistory` (following project convention of default exports for classes).

Import from:
- `../types/index.js` for DiscussionHistoryEntry, Config
- `../utils/ContextOptimizer` for ContextOptimizer
- `../utils/TokenCounter` for TokenCounter

**Update ConversationManager:**

1. Add `import ConversationHistory from './ConversationHistory.js';`
2. Add property: `private history: ConversationHistory;`
3. In constructor, after existing setup, instantiate:
   ```typescript
   this.history = new ConversationHistory(
     this.conversationHistory,
     this.config,
     () => this.currentRound,
     () => this.agentSubstitutions
   );
   ```
4. Replace all internal calls to the moved methods:
   - `this.groupHistoryByRound()` → `this.history.groupHistoryByRound()`
   - `this.compressHistory()` → `this.history.compressHistory()`
   - `this.prepareMessagesForAgent()` → `this.history.prepareMessagesForAgent()`
   - `this.prepareMessagesWithBudget(name)` → `this.history.prepareMessagesWithBudget(name)`
5. Remove the moved methods and caching state from ConversationManager
6. Keep `conversationHistory` array as a public property on ConversationManager (it's accessed by callers and by the judge methods)

**Create `src/core/__tests__/ConversationHistory.test.ts`:**

Unit tests for ConversationHistory in isolation. Mock ContextOptimizer and TokenCounter. Test the behaviors listed in `<behavior>` above.

**Critical constraint:** This is behavior-preserving refactoring. Every method must produce identical output for identical input. Do NOT change any logic, just move it.
  </action>
  <verify>
    <automated>cd /Users/benlamm/Workspace/llm_conclave && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `src/core/ConversationHistory.ts` exists
    - File contains `export default class ConversationHistory`
    - File contains methods: `groupHistoryByRound`, `compressHistory`, `formatEntryAsMessage`, `prepareMessagesForAgent`, `prepareMessagesWithBudget`, `prepareMessagesWithRoundCompression`, `getHistoryTokenThreshold`
    - `src/core/ConversationManager.ts` contains `import ConversationHistory from './ConversationHistory.js'`
    - `src/core/ConversationManager.ts` contains `this.history.groupHistoryByRound()`
    - `src/core/ConversationManager.ts` does NOT contain the method body of `groupHistoryByRound` (grep for `groupHistoryByRound(): {` should match only in ConversationHistory.ts)
    - `src/core/__tests__/ConversationHistory.test.ts` exists with at least 5 test cases
    - All tests pass: `npm test` exits 0
  </acceptance_criteria>
  <done>ConversationHistory class extracted with all 7 history methods. ConversationManager delegates to it. All existing tests pass unchanged.</done>
</task>

</tasks>

<verification>
```bash
# All tests pass
npm test

# ConversationHistory exists and exports correctly
grep "export default class ConversationHistory" src/core/ConversationHistory.ts

# ConversationManager delegates to history
grep "this.history\." src/core/ConversationManager.ts

# Old methods removed from ConversationManager
! grep "groupHistoryByRound(): {" src/core/ConversationManager.ts

# Judge integration tests exist and pass
npx jest --testPathPattern="judge.integration" --no-coverage
```
</verification>

<success_criteria>
- Judge evaluation integration tests with context compression pass (TEST-01)
- ConversationHistory class owns all history manipulation (CONV-01)
- ConversationManager delegates to ConversationHistory
- All 938+ existing tests still pass
- New unit tests for ConversationHistory pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-conversationmanager-decomposition/02-01-SUMMARY.md`
</output>
