---
phase: 09-data-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/consult/logging/ConsultationFileLogger.ts
  - src/consult/logging/__tests__/ConsultationFileLogger.test.ts
  - src/orchestration/ConsultOrchestrator.ts
  - src/orchestration/__tests__/ConsultOrchestrator_EarlyTermination.test.ts
autonomous: true
requirements: [DATA-01, DATA-03]

must_haves:
  truths:
    - "Consult log files are named {consultationId}.json and {consultationId}.md — no double consult- prefix"
    - "ConsultationResult agents array contains provider name (e.g. 'claude', 'openai', 'gemini') not 'unknown' or model name"
  artifacts:
    - path: "src/consult/logging/ConsultationFileLogger.ts"
      provides: "Fixed filename generation without consult- prefix duplication"
      contains: "${result.consultationId}"
    - path: "src/orchestration/ConsultOrchestrator.ts"
      provides: "Provider name from agent.provider.getProviderName()"
      contains: "getProviderName"
  key_links:
    - from: "src/orchestration/ConsultOrchestrator.ts"
      to: "LLMProviderInterface.getProviderName()"
      via: "agent.provider.getProviderName()"
      pattern: "getProviderName\\(\\)"
    - from: "src/consult/logging/ConsultationFileLogger.ts"
      to: "ConsultationResult.consultationId"
      via: "filename = consultationId (no prefix)"
      pattern: "\\$\\{result\\.consultationId\\}"
---

<objective>
Fix duplicate consult log filenames (DATA-01) and populate provider name instead of 'unknown' in ConsultationResult agents array (DATA-03).

Purpose: Consult log files currently get a doubled prefix (consult-consult-{id}) because the consultationId already contains 'consult-' and the filename template prepends another 'consult-'. The agents array in ConsultationResult hardcodes 'unknown' for provider despite LLMProviderInterface exposing getProviderName().
Output: Correctly named log files and accurate provider attribution in consultation results.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/consult/logging/ConsultationFileLogger.ts
@src/orchestration/ConsultOrchestrator.ts
@src/types/index.ts
@src/types/consult.ts
</context>

<interfaces>
<!-- Key types the executor needs -->

From src/types/index.ts:
```typescript
export interface LLMProviderInterface {
  modelName: string;
  chat(messages: Message[], systemPrompt?: string | null, options?: ChatOptions): Promise<ProviderResponse>;
  getProviderName(): string;
  getModelName(): string;
  healthCheck(): Promise<boolean>;
}

export interface Agent {
  name: string;
  provider: LLMProviderInterface;
  systemPrompt: string;
  model: string;
}
```

From src/types/consult.ts:
```typescript
export interface AgentResponse {
  agentId: string;
  agentName: string;
  model: string;
  provider: string;
  content: string;
  tokens: TokenUsage;
  durationMs: number;
  timestamp: string;
  error?: string;
  provider_error?: string;
}
```

Provider name values returned by each provider's getProviderName():
- ClaudeProvider: 'Claude'
- OpenAIProvider: 'OpenAI'
- GeminiProvider: 'Gemini'
- GrokProvider: 'Grok'
- MistralProvider: 'Mistral'
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix duplicate consult- prefix in log filenames (DATA-01)</name>
  <files>src/consult/logging/ConsultationFileLogger.ts, src/consult/logging/__tests__/ConsultationFileLogger.test.ts</files>
  <read_first>src/consult/logging/ConsultationFileLogger.ts, src/consult/logging/__tests__/ConsultationFileLogger.test.ts, src/orchestration/ConsultOrchestrator.ts (line 1504 generateId method)</read_first>
  <action>
The bug: `generateId('consult')` produces IDs like `consult-lz5abc-xyz1234`. Then `writeJsonLog` and `writeMarkdownLog` build filenames as `consult-${result.consultationId}` which becomes `consult-consult-lz5abc-xyz1234.json`.

Fix in ConsultationFileLogger.ts:
- In `writeJsonLog` (line 103): Change `const filename = \`consult-${result.consultationId}.json\`` to `const filename = \`${result.consultationId}.json\``
- In `writeMarkdownLog` (line 134): Change `const filename = \`consult-${result.consultationId}.md\`` to `const filename = \`${result.consultationId}.md\``

The consultationId already has the `consult-` prefix from `generateId('consult')`, so the filename template must NOT add another one.

Add/update tests in ConsultationFileLogger.test.ts:
- Add a test that verifies when consultationId is 'consult-abc-123', the JSON file is written to `consult-abc-123.json` (not `consult-consult-abc-123.json`)
- Add a test that verifies the markdown file is written to `consult-abc-123.md` (not `consult-consult-abc-123.md`)
  </action>
  <verify>
    <automated>cd /Users/benlamm/Workspace/llm_conclave && npx jest src/consult/logging/__tests__/ConsultationFileLogger.test.ts --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
- grep -n 'consult-\${result.consultationId}' src/consult/logging/ConsultationFileLogger.ts returns NO matches
- grep -n '\${result.consultationId}.json' src/consult/logging/ConsultationFileLogger.ts returns a match on the writeJsonLog method
- grep -n '\${result.consultationId}.md' src/consult/logging/ConsultationFileLogger.ts returns a match on the writeMarkdownLog method
- All ConsultationFileLogger tests pass
  </acceptance_criteria>
  <done>Log files are named {consultationId}.json and {consultationId}.md without double consult- prefix</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded 'unknown' provider with actual provider name (DATA-03)</name>
  <files>src/orchestration/ConsultOrchestrator.ts</files>
  <read_first>src/orchestration/ConsultOrchestrator.ts (lines 407-414, 828-838, 1145-1158, 1349-1360, 1540-1560, 1655-1670), src/types/index.ts (LLMProviderInterface, Agent)</read_first>
  <action>
The bug: ConsultOrchestrator maps `provider: 'unknown'` in 6 locations instead of using the available `agent.provider.getProviderName()`.

Fix all occurrences. Use `.toLowerCase()` on the result for consistent lowercase naming (requirement says "anthropic" not "Claude"):

1. Line ~412 (consultation:started event agents mapping):
   Change: `agents: (this.agents || []).map(a => ({ name: a.name, model: a.model, provider: 'unknown' }))`
   To: `agents: (this.agents || []).map(a => ({ name: a.name, model: a.model, provider: a.provider.getProviderName().toLowerCase() }))`

2. Line ~833 (error response in cross-exam):
   Change: `provider: 'unknown'`
   To: `provider: agent.provider?.getProviderName().toLowerCase() || 'unknown'`

3. Line ~1150 (agentConfig for hedged request in cross-exam):
   Change: `provider: agent.model`
   To: `provider: agent.provider.getProviderName().toLowerCase()`

4. Line ~1222 (agentResponse in cross-exam success):
   Change: `provider: response.provider || 'unknown'`
   To: `provider: response.provider || agent.provider.getProviderName().toLowerCase()`

5. Line ~1357 (agentConfig for hedged request in round 1):
   Change: `provider: agent.model`
   To: `provider: agent.provider.getProviderName().toLowerCase()`

6. Line ~1453 (agentResponse in round 1 success):
   Already uses `response.provider || 'unknown'` — change fallback:
   `provider: response.provider || agent.provider.getProviderName().toLowerCase()`

7. Line ~1485 (error agentResponse in round 1):
   Change: `provider: 'unknown'`
   To: `provider: agent.provider?.getProviderName().toLowerCase() || 'unknown'`

8. Line ~1552 (createFinalResult agents mapping):
   Change: `agents: this.agents.map(a => ({ name: a.name, model: a.model, provider: 'unknown' }))`
   To: `agents: this.agents.map(a => ({ name: a.name, model: a.model, provider: a.provider.getProviderName().toLowerCase() }))`

9. Line ~1666 (createResult agents mapping):
   Change: `agents: this.agents.map(a => ({ name: a.name, model: a.model, provider: 'unknown' }))`
   To: `agents: this.agents.map(a => ({ name: a.name, model: a.model, provider: a.provider.getProviderName().toLowerCase() }))`

IMPORTANT: The `provider: agent.model` lines in agentConfig objects (items 3 and 5) are passed to HedgedRequestManager which uses them as provider IDs for ProviderFactory. This is a separate concern — HedgedRequestManager uses it as a ProviderFactory key, not as a display name. However, the model name IS the key ProviderFactory uses to create providers (e.g., 'claude-sonnet-4-5'). So for HedgedRequestManager's agentConfig.provider field, keep using `agent.model` since that's what ProviderFactory needs. ONLY change the AgentResponse.provider fields and the ConsultationResult.agents mappings.

Corrected list — change ONLY these:
- Items 1, 2, 4, 6, 7, 8, 9 (display/result provider fields)
- Do NOT change items 3 and 5 (HedgedRequestManager agentConfig.provider) — those must remain `agent.model` for ProviderFactory routing
  </action>
  <verify>
    <automated>cd /Users/benlamm/Workspace/llm_conclave && npx jest src/orchestration/__tests__/ --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
- grep -c "provider: 'unknown'" src/orchestration/ConsultOrchestrator.ts returns 0
- grep -c "getProviderName" src/orchestration/ConsultOrchestrator.ts returns at least 5
- The two agentConfig objects for HedgedRequestManager still use `provider: agent.model` (not changed)
- All orchestrator tests pass
  </acceptance_criteria>
  <done>ConsultationResult agents array and all AgentResponse objects carry actual provider names (claude, openai, gemini, grok, mistral) instead of 'unknown'</done>
</task>

</tasks>

<verification>
1. `grep -rn "consult-\\\${" src/consult/logging/ConsultationFileLogger.ts` — should find NO matches with double prefix
2. `grep -c "provider: 'unknown'" src/orchestration/ConsultOrchestrator.ts` — should return 0
3. `npm run build` compiles without errors
4. `npx jest src/consult/logging/__tests__/ConsultationFileLogger.test.ts src/orchestration/__tests__/ --no-coverage` — all tests pass
</verification>

<success_criteria>
- Log files use `{consultationId}.json` and `{consultationId}.md` without prefix duplication
- All provider fields in ConsultationResult and AgentResponse use actual provider names
- All existing tests pass, new tests cover the filename fix
</success_criteria>

<output>
After completion, create `.planning/phases/09-data-correctness/09-01-SUMMARY.md`
</output>
